%h1 Winelists#show
%p Find me in app/views/homepage/winelists/show.html.haml
      -# - @testimages.each do |testimage|
      -#   = image_tag testimage.image_url

      -# - @wines.each do |wine|
      -#   = wine.id
      -#   - wine.sepages.each do |sepage|
      -#     = sepage.name
      -#   - wine.productimages.each do |productimage|
      -#     = image_tag productimage.image_url
      -#   = wine.name
      -#   = wine.wine_type.group
      -#   = wine.domaine.name
